add <- function(a, b) {
  a + b
}

greet <- function(name) {
  paste("Hello,", name, "!")
}

multiply <- function(a, b) {
  a * b
}

calculate_mean <- function(values) {
  sum(values) / length(values)
}
